DOERCHAT_WEBHOOK_SECRET=DSepDk8ioa5Tz7d8kUJKks2oM6Ce5bxHppD4+8pHhQw=

# Redis (required)
REDIS_URL=redis://127.0.0.1:6379

# SMTP bind
SMTP_HOST=0.0.0.0
SMTP_PORT=25

# SMTP limits/timeouts
SMTP_MAX_CLIENTS=200
SMTP_SOCKET_TIMEOUT_MS=120000
SMTP_CLOSE_TIMEOUT_MS=30000
MAX_EMAIL_SIZE_BYTES=20971520

# Rate limiting (per IP and per MAIL FROM)
EMAIL_RATE_LIMIT_WINDOW_SECONDS=300
EMAIL_RATE_LIMIT_MAX=1000

# Queue reliability
EMAIL_JOB_ATTEMPTS=8
EMAIL_JOB_BACKOFF_MS=5000
EMAIL_JOB_RETENTION_SECONDS=86400
EMAIL_JOB_FAIL_RETENTION_SECONDS=604800

# Worker behavior
WORKER_CONCURRENCY=5
DOERCHAT_API_TIMEOUT_MS=10000

# Spam checks
SPAM_THRESHOLD=5.0
SPAMASSASSIN_TIMEOUT_MS=30000

# Spooling (server writes .eml files; worker reads and deletes them)
EMAIL_SPOOL_DIR=.spool
EMAIL_SPOOL_MAX_AGE_SECONDS=604800
EMAIL_SPOOL_CLEAN_INTERVAL_MS=3600000
